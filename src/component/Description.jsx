import React from "react";

const Description = ({ description }) => {
    return (
        <div className="desc">
            <p>{description}</p>
        </div>
    );
};

export default Description;
